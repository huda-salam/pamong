package bad

// Modul bad TIDAK mendeklarasikan Imports apa pun. Literal di bawah adalah permission
// miliknya sendiri (prefix "bad") sehingga tidak dilaporkan walau berupa string.
var _own = "bad:doc:buat"
